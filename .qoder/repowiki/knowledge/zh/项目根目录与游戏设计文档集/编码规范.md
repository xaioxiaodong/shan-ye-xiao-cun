- GDD 中以 Markdown 代码块形式直接给出 TypeScript interface / const 常量，作为后续实现的类型与数据源，实现“文档即接口定义”。
- 各系统章节之间使用 `→ X.Y` 形式的锚点交叉引用，强制在编写时维护系统间的关联闭环。
- 所有数值类设计都附带 `// 安全保护：...` 注释并指向 11.4.x 七维熔断保护条款，将数值校验与安全护栏绑定到同一处说明。