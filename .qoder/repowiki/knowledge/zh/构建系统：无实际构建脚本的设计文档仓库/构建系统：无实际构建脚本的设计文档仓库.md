---
kind: build_system
name: 构建系统：无实际构建脚本的设计文档仓库
category: build_system
scope:
    - '**'
---

该仓库是像素农场游戏《山野小村》的**纯设计文档仓库**，不包含任何可编译的代码或构建系统。经全面扫描，仓库根目录仅包含以下文件：

- `.claude/` — Claude AI 工作流与记忆配置（CLAUDE.md、memory/*.md）
- `.gitignore` — Git 忽略规则
- `gdd.md` — 游戏设计文档（91.6KB）
- `star-dew-valley-complete-analysis.md` — 星露谷系统参考拆解（132.7KB）

**未找到任何构建相关文件或配置**：Makefile、build.sh、Dockerfile、docker-compose.yml、package.json、go.mod、Cargo.toml、CMakeLists.txt、setup.py、requirements.txt、pom.xml、build.gradle、sbt.sbt、Gemfile、Rakefile、Taskfile、justfile、Brewfile、Vagrantfile、vite.config、webpack.config、tsconfig.json、pyproject.toml、uv.toml、poetry.lock、Pipfile、tox.ini、noxfile、conanfile、meson.build、ninja、CMake、Bazel、BUILD、WORKSPACE、MODULE.bazel、rules_go、rules_python、rules_rust、rust-toolchain、rustfmt、clippy、cargo、npm、yarn、pnpm、bun、pip、conda、mamba、venv、virtualenv、pyenv、rbenv、asdf、mise、fnm、volta、node、python、java、ruby、go、rust、c++、clang、gcc、msvc、zig、nim、lua、julia、r、scala、kotlin、swift、dart、flutter、Android、iOS、macOS、Windows、Linux 等构建工具链、CI 配置文件（GitHub Actions、GitLab CI、CircleCI、Travis、AppVeyor、Jenkins、TeamCity、Drone、Woodpecker）、部署配置（ArgoCD、Helm、K8s、Terraform、Ansible、Puppet、Chef、SaltStack）均不存在。

该仓库仅用于存放游戏设计文档和参考分析，不涉及代码编译、测试、打包或部署流程。当实际游戏代码仓库建立时，需另行引入构建系统。